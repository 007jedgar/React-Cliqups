import {
  scale,
  moderateScale,
  verticalScale,
  ScaledSheet,
} from 'react-native-size-matters'


export const general = ScaledSheet.create({
  container: {
    flex: 1,
    marginTop: '20@ms',
  },
})
